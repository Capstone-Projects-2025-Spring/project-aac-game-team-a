
/**
 * Handles the paths to images to be referenced by the drawer
 */
class ReferenceImageProvider{
    /**
     * Finds the path to an image when called
     * 
     * @param {String} word The word associated with a prompt
     * @param {String[]} excludeImages Images that have already been used once during the game 
     */
    getImageForWord(word, excludeImages){

    }
}
